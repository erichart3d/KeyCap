[package]
name = "keycap-recorder"
version = "0.1.0"
edition = "2021"
description = "Windows native recorder sidecar for KeyCap"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

